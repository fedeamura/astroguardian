name: repository
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  hive: 2.2.3
  hive_flutter: 1.1.0
  uuid: 4.3.3
  path: 1.8.3
  path_provider: 2.1.2
  synchronized: 3.1.0+1

  model:
    path: ../model

  adapter:
    path: ../adapter

dev_dependencies:
  flutter_lints: 3.0.1

flutter:

